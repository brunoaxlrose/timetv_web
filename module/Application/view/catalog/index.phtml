<?php
$totalCount = 0;
if ($this->search) {
    $totalCount = count($this->items);
} else {
    if ($this->grouped) {
        $totalCount = count($this->items['watching']) + count($this->items['up_to_date']) + count($this->items['completed']);
    } else {
        $totalCount = count($this->items);
    }
}

$this->partial('catalog/css.phtml', ['page' => 'catalog']);
$this->partial('catalog/js.phtml', ['page' => 'catalog']);
?>

<!-- Discover Search Bar -->
<div class="discover-search-wrap">
    <!-- No form action/method: JS intercepts keyup/input and sends via $.ajax POST -->
    <div id="searchForm" class="d-flex gap-2">
        <div class="input-group">
            <span class="input-group-text border-0" style="background-color: #1c1c1e !important; color: #a88beb !important;"><i class="bi bi-search"></i></span>
            <input type="text" id="searchInput" name="search" class="form-control discover-search-input" placeholder="Pesquisar séries, filmes, animes..." value="<?= htmlspecialchars($this->search) ?>" autocomplete="off">
            <button type="button" class="btn border-0" onclick="openFilterModal()" style="background-color: #1c1c1e !important; padding-right: 15px;"><i class="bi bi-sliders text-gray"></i></button>
        </div>
        <?php if (!empty($this->search) || !empty($this->statusFilter) || !$this->mediaMovies || !$this->mediaSeries || !$this->mediaAnime): ?>
            <a href="/catalog" class="btn btn-outline-secondary rounded-pill d-flex align-items-center justify-content-center px-3" style="border-color: var(--border-dark) !important; font-size: 0.8rem;">Limpar</a>
        <?php endif; ?>
    </div>
</div>

<div class="px-3 py-2">
    <!-- Header of page matching Screen 3 -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div class="d-flex align-items-baseline gap-2">
            <h4 class="text-white mb-0" style="font-weight: 800; font-size: 1.5rem; letter-spacing: -0.5px;">Coleção</h4>
            <span class="small" style="font-size: 0.75rem; font-weight: 600; color: #a0a0c0 !important;"><?= $totalCount ?> itens</span>
        </div>
        
        <div class="d-flex align-items-center gap-3">
            <!-- Grid Layout Toggle Icon -->
            <i class="bi <?= $this->grouped ? 'bi-grid-3x3-gap' : 'bi-grid-3x3-gap-fill text-warning' ?>" style="cursor: pointer; font-size: 1.1rem; color: #a5a5c0;" onclick="toggleGroupedLayout()" title="Alternar agrupamento"></i>
            <!-- Sliders/Filter Modal Icon -->
            <i class="bi bi-sliders" style="cursor: pointer; font-size: 1.1rem; color: #a5a5c0;" onclick="openFilterModal()" title="Filtrar & Ordenar"></i>
            <!-- Book/Diary History Icon -->
            <a href="/diary" class="text-decoration-none" style="color: #a5a5c0;"><i class="bi bi-book" style="cursor: pointer; font-size: 1.1rem;" title="O Meu Diário"></i></a>
            <!-- Media Types Filter Icon -->
            <i class="bi bi-folder" style="cursor: pointer; font-size: 1.1rem; color: #a5a5c0;" onclick="openMediaTypeModal()" title="Tipos de Mídia"></i>
        </div>
    </div>

    <!-- Items Grid matching Refract Coleção style -->
    <div class="mt-2">
        <!-- Skeleton Loading Container -->
        <div id="skeletonContainer" class="d-none">
            <div class="row row-cols-3 g-2">
                <?php for ($i = 0; $i < 6; $i++): ?>
                    <div class="col mb-3">
                        <div class="refract-skeleton-card"></div>
                        <div class="refract-skeleton-text title"></div>
                        <div class="refract-skeleton-text meta"></div>
                    </div>
                <?php endfor; ?>
            </div>
        </div>

        <div id="resultsGrid">
            <?php if ($this->search): ?>
                <!-- Search mode: display flat results -->
                <?php if (empty($this->items)): ?>
                    <div class="text-center py-5">
                        <div class="mb-3 text-muted" style="font-size: 3rem;">
                            <i class="bi bi-search"></i>
                        </div>
                        <h6 class="text-white fw-bold">Nenhum show encontrado</h6>
                        <p class="text-gray small px-3">Tente pesquisar por outros termos na caixa de busca global acima.</p>
                    </div>
                <?php else: ?>
                    <div class="row row-cols-3 g-2" id="searchResultGrid">
                    <?php $itemIndex = 0; foreach ($this->items as $item): $itemIndex++; ?>
                        <div class="col mb-3 refract-search-row <?= $itemIndex > 20 ? 'd-none' : '' ?>">
                            <div class="refract-poster-card position-relative overflow-hidden rounded-3 shadow-sm" style="aspect-ratio: 2/3;">
                                <?php
                                    if ($item['id_item']) $detailLink = '/detail?id=' . $item['id_item'];
                                    elseif (!empty($item['tvmaze_id'])) $detailLink = '/detail?tvmaze_id=' . $item['tvmaze_id'];
                                    elseif (!empty($item['tmdb_id'])) $detailLink = '/detail?tmdb_id=' . $item['tmdb_id'];
                                    elseif (!empty($item['mal_id'])) $detailLink = '/detail?mal_id=' . $item['mal_id'];
                                    else $detailLink = '#';
                                ?>
                                <a href="<?= $detailLink ?>">
                                    <img src="<?= htmlspecialchars($item['poster_url']) ?>" class="refract-poster-img w-100 h-100 object-fit-cover" alt="poster" onerror="this.src='https://images.unsplash.com/photo-1594909122845-11baa439b7bf?q=80&w=400'; this.onerror=null;">
                                </a>
                                <div class="position-absolute top-0 start-0 w-100 d-flex justify-content-between p-2" style="background: linear-gradient(to bottom, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0) 100%);">
                                    <span class="badge bg-black bg-opacity-70 p-1 d-flex align-items-center justify-content-center" style="width: 18px; height: 18px; border-radius: 4px;">
                                        <i class="bi <?= $item['type'] === 'movie' ? 'bi-film' : 'bi-tv' ?>" style="font-size: 0.6rem;"></i>
                                    </span>
                                    <?php if (!$item['id_item']): ?>
                                        <span class="badge bg-danger p-1" style="font-size: 0.52rem; font-weight: 800; border-radius: 4px; line-height: 1;">NEW</span>
                                    <?php endif; ?>
                                </div>
                            </div>
                            <div class="px-1 text-start mt-1">
                                <a href="<?= $detailLink ?>" class="text-decoration-none">
                                    <div class="text-white fw-bold text-truncate" style="font-size: 0.72rem; line-height: 1.2;"><?= htmlspecialchars($item['title']) ?></div>
                                </a>
                                <div style="font-size: 0.62rem; margin-top: 1px; color: #a5a5c0;"><?= $item['release_year'] ?></div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                    </div>
                    <?php if (count($this->items) > 20): ?>
                        <div class="text-center w-100 mt-4 py-3 btn-show-more-search-container">
                            <button class="btn btn-outline-secondary rounded-pill px-4 btn-show-more-search" onclick="showMoreSearchResults(this)" style="border-color: rgba(255,255,255,0.15) !important; color: var(--text-gray); font-weight: 600; font-size: 0.75rem; background-color: rgba(255,255,255,0.03);">Ver mais resultados</button>
                        </div>
                    <?php endif; ?>
                <?php endif; ?>

        <?php else: ?>
            <!-- Popular and Upcoming Shows -->
            <div class="mb-5 mt-2">
                <h5 class="refract-section-title d-flex align-items-center gap-2 mb-3">
                    <i class="bi bi-fire text-danger animate-pulse"></i> Mais Populares
                </h5>
                <div class="row row-cols-3 row-cols-md-4 row-cols-lg-6 g-3">
                    <?php foreach ($this->popularItems as $item): ?>
                        <?php
                            $detailLink = '#';
                            if (!empty($item['tvmaze_id'])) $detailLink = '/detail?tvmaze_id=' . $item['tvmaze_id'];
                            elseif (!empty($item['tmdb_id'])) $detailLink = '/detail?tmdb_id=' . $item['tmdb_id'];
                            elseif (!empty($item['mal_id'])) $detailLink = '/detail?mal_id=' . $item['mal_id'];
                        ?>
                        <div class="col mb-3">
                            <div class="refract-poster-card position-relative overflow-hidden rounded-3 shadow-sm" style="aspect-ratio: 2/3;">
                                <a href="<?= $detailLink ?>">
                                    <img src="<?= htmlspecialchars($item['poster_url']) ?>" class="refract-poster-img w-100 h-100 object-fit-cover" alt="poster" onerror="this.src='https://images.unsplash.com/photo-1594909122845-11baa439b7bf?q=80&w=400'; this.onerror=null;">
                                </a>
                                <div class="position-absolute top-0 start-0 w-100 d-flex justify-content-between p-2" style="background: linear-gradient(to bottom, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0) 100%);">
                                    <span class="badge bg-black bg-opacity-70 p-1 d-flex align-items-center justify-content-center" style="width: 18px; height: 18px; border-radius: 4px;">
                                        <i class="bi <?= $item['type'] === 'movie' ? 'bi-film' : 'bi-tv' ?>" style="font-size: 0.6rem;"></i>
                                    </span>
                                </div>
                            </div>
                            <div class="px-1 text-start mt-1">
                                <a href="<?= $detailLink ?>" class="text-decoration-none">
                                    <div class="text-white fw-bold text-truncate" style="font-size: 0.72rem; line-height: 1.2;"><?= htmlspecialchars($item['title']) ?></div>
                                </a>
                                <div style="font-size: 0.62rem; margin-top: 1px; color: #a5a5c0;"><?= $item['release_year'] ?></div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>

            <div class="mb-5">
                <h5 class="refract-section-title d-flex align-items-center gap-2 mb-3">
                    <i class="bi bi-calendar-event text-info"></i> Em Breve
                </h5>
                <div class="row row-cols-3 row-cols-md-4 row-cols-lg-6 g-3">
                    <?php foreach ($this->upcomingItems as $item): ?>
                        <?php
                            $detailLink = '#';
                            if (!empty($item['tvmaze_id'])) $detailLink = '/detail?tvmaze_id=' . $item['tvmaze_id'];
                            elseif (!empty($item['tmdb_id'])) $detailLink = '/detail?tmdb_id=' . $item['tmdb_id'];
                            elseif (!empty($item['mal_id'])) $detailLink = '/detail?mal_id=' . $item['mal_id'];
                        ?>
                        <div class="col mb-3">
                            <div class="refract-poster-card position-relative overflow-hidden rounded-3 shadow-sm" style="aspect-ratio: 2/3;">
                                <a href="<?= $detailLink ?>">
                                    <img src="<?= htmlspecialchars($item['poster_url']) ?>" class="refract-poster-img w-100 h-100 object-fit-cover" alt="poster" onerror="this.src='https://images.unsplash.com/photo-1594909122845-11baa439b7bf?q=80&w=400'; this.onerror=null;">
                                </a>
                                <div class="position-absolute top-0 start-0 w-100 d-flex justify-content-between p-2" style="background: linear-gradient(to bottom, rgba(0,0,0,0.6) 0%, rgba(0,0,0,0) 100%);">
                                    <span class="badge bg-black bg-opacity-70 p-1 d-flex align-items-center justify-content-center" style="width: 18px; height: 18px; border-radius: 4px;">
                                        <i class="bi <?= $item['type'] === 'movie' ? 'bi-film' : 'bi-tv' ?>" style="font-size: 0.6rem;"></i>
                                    </span>
                                </div>
                            </div>
                            <div class="px-1 text-start mt-1">
                                <a href="<?= $detailLink ?>" class="text-decoration-none">
                                    <div class="text-white fw-bold text-truncate" style="font-size: 0.72rem; line-height: 1.2;"><?= htmlspecialchars($item['title']) ?></div>
                                </a>
                                <div style="font-size: 0.62rem; margin-top: 1px; color: #a5a5c0;">
                                    <?php if (!empty($item['release_date'])): ?>
                                        Estreia: <?= date('d/m/Y', strtotime($item['release_date'])) ?>
                                    <?php else: ?>
                                        <?= $item['release_year'] ?>
                                    <?php endif; ?>
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
        <?php endif; ?>
        </div> <!-- end of resultsGrid -->
    </div>
</div>

<!-- Advanced Filter Modal (Screen 2 Layout) -->
<div id="filterModal" class="tvtime-modal-overlay">
    <div class="tvtime-modal-sheet text-start" style="border-radius: 20px 20px 0 0; background-color: #1e1e2d; border-top: 1px solid #2d2d3e; max-width: 440px;">
        <div class="d-flex align-items-center justify-content-between mb-1">
            <span class="text-white fw-bold" style="font-size: 1.1rem;">Filtrar & Ordenar</span>
            <div class="d-flex align-items-center gap-2">
                <span class="text-muted small me-2" style="cursor: pointer; font-size: 0.8rem;" onclick="resetFilters()">Repor tudo</span>
                <button type="button" class="btn-close btn-close-white" onclick="closeFilterModal()"></button>
            </div>
        </div>
        <div class="text-muted small mb-4" style="font-size: 0.8rem;">Personaliza a visualização da tua coleção.</div>

        <form id="filterForm" action="/catalog" method="GET">
            <!-- Keep search and media type parameters -->
            <input type="hidden" name="search" value="<?= htmlspecialchars($this->search) ?>">
            <input type="hidden" name="media_movies" value="<?= $this->mediaMovies ? '1' : '0' ?>">
            <input type="hidden" name="media_series" value="<?= $this->mediaSeries ? '1' : '0' ?>">
            <input type="hidden" name="media_anime" value="<?= $this->mediaAnime ? '1' : '0' ?>">

            <!-- Agrupado por estado -->
            <div class="d-flex align-items-center justify-content-between mb-4">
                <div>
                    <span class="text-white small fw-bold">Agrupado por estado</span>
                    <div class="text-muted" style="font-size: 0.65rem; margin-top: 1px;">Organiza a tua biblioteca em secções por estado.</div>
                </div>
                <div class="form-check form-switch p-0 m-0">
                    <input class="form-check-input" type="checkbox" name="grouped" value="1" <?= $this->grouped ? 'checked' : '' ?> style="width: 38px; height: 18px;">
                </div>
            </div>

            <!-- Filtrar por estado -->
            <h6 class="text-gray small fw-bold text-uppercase mb-3" style="font-size: 0.65rem; letter-spacing: 0.5px;">Filtrar por estado</h6>
            <div class="d-flex flex-wrap gap-2 mb-4">
                <input type="radio" class="btn-check" name="status_filter" id="statusFilterNone" value="" <?= empty($this->statusFilter) ? 'checked' : '' ?>>
                <label class="pill-btn m-0" for="statusFilterNone">Tudo</label>

                <input type="radio" class="btn-check" name="status_filter" id="statusFilterWatching" value="watching" <?= $this->statusFilter === 'watching' ? 'checked' : '' ?>>
                <label class="pill-btn m-0" for="statusFilterWatching">A ver</label>

                <input type="radio" class="btn-check" name="status_filter" id="statusFilterUpToDate" value="em_dia" <?= $this->statusFilter === 'em_dia' ? 'checked' : '' ?>>
                <label class="pill-btn m-0" for="statusFilterUpToDate">Em dia</label>

                <input type="radio" class="btn-check" name="status_filter" id="statusFilterCompleted" value="visto" <?= $this->statusFilter === 'visto' ? 'checked' : '' ?>>
                <label class="pill-btn m-0" for="statusFilterCompleted">Visto</label>
            </div>

            <!-- Ordenar por -->
            <h6 class="text-gray small fw-bold text-uppercase mb-3" style="font-size: 0.65rem; letter-spacing: 0.5px;">Ordenar por</h6>
            <div class="refract-card-dark p-3 rounded-4 d-flex flex-column gap-3 mb-4">
                <div class="d-flex align-items-center justify-content-between">
                    <label class="text-white small fw-bold m-0" for="sortLastWatched">Último visto</label>
                    <input class="form-check-input bg-transparent border-secondary" type="radio" name="sort_by" id="sortLastWatched" value="last_watched" <?= $this->sortBy === 'last_watched' ? 'checked' : '' ?>>
                </div>
                <div class="d-flex align-items-center justify-content-between" style="border-top: 1px solid #2d2d3e; padding-top: 12px;">
                    <label class="text-white small fw-bold m-0" for="sortLastAdded">Último adicionado</label>
                    <input class="form-check-input bg-transparent border-secondary" type="radio" name="sort_by" id="sortLastAdded" value="last_added" <?= $this->sortBy === 'last_added' ? 'checked' : '' ?>>
                </div>
                <div class="d-flex align-items-center justify-content-between" style="border-top: 1px solid #2d2d3e; padding-top: 12px;">
                    <label class="text-white small fw-bold m-0" for="sortLastPremiered">Última estreia</label>
                    <input class="form-check-input bg-transparent border-secondary" type="radio" name="sort_by" id="sortLastPremiered" value="last_premiered" <?= $this->sortBy === 'last_premiered' ? 'checked' : '' ?>>
                </div>
            </div>

            <!-- Action Button -->
            <button type="submit" class="btn w-100 py-3 rounded-pill fw-bold text-white mb-2" style="background-color: #7c4dff !important; border: none; font-size: 0.85rem;">Aplicar filtros</button>
        </form>
    </div>
</div>

<!-- Media Type Modal Overlay (Screen 3 Layout) -->
<div id="mediaTypeModal" class="tvtime-modal-overlay">
    <div class="tvtime-modal-sheet text-start" style="border-radius: 20px 20px 0 0; background-color: #1e1e2d; border-top: 1px solid #2d2d3e; max-width: 440px;">
        <div class="d-flex align-items-center justify-content-between mb-1">
            <span class="text-white fw-bold" style="font-size: 1.1rem;">Tipos de mídia</span>
            <button type="button" class="btn-close btn-close-white" onclick="closeMediaTypeModal()"></button>
        </div>
        <div class="text-muted small mb-4" style="font-size: 0.8rem;">Filtra que tipos de mídia aparecem apenas nesta vista.</div>

        <form id="mediaTypeForm" action="/catalog" method="GET">
            <!-- Keep search and filter parameters -->
            <input type="hidden" name="search" value="<?= htmlspecialchars($this->search) ?>">
            <input type="hidden" name="grouped" value="<?= $this->grouped ? '1' : '0' ?>">
            <input type="hidden" name="status_filter" value="<?= htmlspecialchars($this->statusFilter) ?>">
            <input type="hidden" name="sort_by" value="<?= htmlspecialchars($this->sortBy) ?>">

            <div class="refract-card-dark p-3 rounded-4 d-flex flex-column gap-3 mb-4">
                <!-- Filmes -->
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center gap-3">
                        <div class="d-flex align-items-center justify-content-center rounded" style="width: 32px; height: 32px; background-color: rgba(54, 162, 235, 0.15);">
                            <i class="bi bi-film text-info" style="font-size: 1.1rem;"></i>
                        </div>
                        <span class="text-white small fw-bold">Filmes</span>
                    </div>
                    <div class="form-check form-switch p-0 m-0">
                        <!-- Keep a hidden input for unchecked state to send 0 -->
                        <input type="hidden" name="media_movies" value="0">
                        <input class="form-check-input" type="checkbox" name="media_movies" value="1" <?= $this->mediaMovies ? 'checked' : '' ?> style="width: 38px; height: 18px;">
                    </div>
                </div>

                <!-- Séries -->
                <div class="d-flex align-items-center justify-content-between" style="border-top: 1px solid #2d2d3e; padding-top: 12px;">
                    <div class="d-flex align-items-center gap-3">
                        <div class="d-flex align-items-center justify-content-center rounded" style="width: 32px; height: 32px; background-color: rgba(255, 159, 64, 0.15);">
                            <i class="bi bi-tv text-warning" style="font-size: 1.1rem;"></i>
                        </div>
                        <span class="text-white small fw-bold">Séries</span>
                    </div>
                    <div class="form-check form-switch p-0 m-0">
                        <input type="hidden" name="media_series" value="0">
                        <input class="form-check-input" type="checkbox" name="media_series" value="1" <?= $this->mediaSeries ? 'checked' : '' ?> style="width: 38px; height: 18px;">
                    </div>
                </div>

                <!-- Anime -->
                <div class="d-flex align-items-center justify-content-between" style="border-top: 1px solid #2d2d3e; padding-top: 12px;">
                    <div class="d-flex align-items-center gap-3">
                        <div class="d-flex align-items-center justify-content-center rounded" style="width: 32px; height: 32px; background-color: rgba(168, 139, 235, 0.15);">
                            <i class="bi bi-stars" style="color: #a88beb; font-size: 1.1rem;"></i>
                        </div>
                        <span class="text-white small fw-bold">Anime</span>
                    </div>
                    <div class="form-check form-switch p-0 m-0">
                        <input type="hidden" name="media_anime" value="0">
                        <input class="form-check-input" type="checkbox" name="media_anime" value="1" <?= $this->mediaAnime ? 'checked' : '' ?> style="width: 38px; height: 18px;">
                    </div>
                </div>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn w-100 py-3 rounded-pill fw-bold text-white mb-2" style="background-color: #7c4dff !important; border: none; font-size: 0.85rem;">Aplicar filtros</button>
        </form>
    </div>
</div>

