<?php
if (isset($page)) {
    $version = time();
    $this->headLink()->appendStylesheet("/css/app/catalog/{$page}.css?v={$version}");
    echo '<link rel="stylesheet" href="/css/app/catalog/' . $page . '.css?v=' . $version . '">';
}
?>
