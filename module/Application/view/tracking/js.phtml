<?php
if (isset($page)) {
    $script = ($page === 'dashboard')
        ? '/js/app/catalog/catalog.js?v=' . time()
        : "/js/app/tracking/{$page}.js?v=" . time();
    $this->inlineScript()->appendFile($script);
}
?>
