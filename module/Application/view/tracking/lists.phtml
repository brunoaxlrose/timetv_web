<?php
$userLists = $this->userLists ?? [];
?>

<style>
    .refract-lists-container {
        font-family: 'Outfit', 'Inter', -apple-system, sans-serif;
        color: #f2f2f7;
    }
    .list-summary-card {
        background: linear-gradient(180deg, #1d1d2a 0%, #171720 100%);
        border: 1px solid rgba(255,255,255,0.05);
        border-radius: 16px;
        padding: 16px;
        cursor: pointer;
        transition: transform 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
        overflow: hidden;
    }
    .list-summary-card:hover {
        transform: translateY(-2px);
        border-color: rgba(168,139,235,0.35);
        box-shadow: 0 12px 24px rgba(0,0,0,0.25);
    }
    .list-summary-cover {
        width: 56px;
        height: 80px;
        min-width: 56px;
        max-width: 56px;
        display: block;
        flex: 0 0 56px;
        border-radius: 10px;
        object-fit: cover;
        border: 1px solid rgba(255,255,255,0.05);
        background: #12121a;
    }
    .list-summary-cover--empty {
        display: flex;
        align-items: center;
        justify-content: center;
        color: #a88beb;
        font-size: 1.25rem;
    }
    .list-summary-body {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        width: 100%;
    }
    .list-summary-main {
        display: flex;
        align-items: center;
        gap: 12px;
        min-width: 0;
        flex: 1 1 auto;
    }
    .list-summary-meta {
        min-width: 0;
        flex: 1 1 auto;
    }
    .list-modal-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 10px;
    }
    .list-modal-poster {
        width: 100%;
        aspect-ratio: 2 / 3;
        object-fit: cover;
        border-radius: 10px;
        border: 1px solid rgba(255,255,255,0.06);
        background: #12121a;
    }
    .list-modal-title {
        color: #fff;
        font-size: 0.72rem;
        line-height: 1.25;
        margin-top: 6px;
        min-height: 34px;
    }
</style>

<div class="px-3 refract-lists-container pb-5">
    <div class="d-flex justify-content-between align-items-center mt-3 mb-4">
        <h4 class="text-white fw-bold mb-0" style="font-size: 1.5rem; letter-spacing: -0.5px;">As tuas listas</h4>
        <button class="btn btn-sm text-white rounded-pill px-3 py-1.5 fw-bold d-flex align-items-center gap-1" style="background-color: #7c4dff;" onclick="promptCreateList()">
            <i class="bi bi-plus-lg"></i> Criar lista
        </button>
    </div>

    <?php if (empty($userLists)): ?>
        <div class="text-center py-5">
            <div class="mb-3 text-muted" style="font-size: 3.5rem;">
                <i class="bi bi-folder2-open"></i>
            </div>
            <h6 class="text-white fw-bold">Nenhuma lista criada</h6>
            <p class="text-gray small px-4 mb-3">Crie listas personalizadas para organizar seus filmes, séries, animes e novelas.</p>
        </div>
    <?php else: ?>
        <div class="d-flex flex-column gap-3">
            <?php foreach ($userLists as $list): ?>
                <div class="list-summary-card" onclick="openListItemsModal(<?= (int)$list['id_lista'] ?>)">
                    <div class="list-summary-body">
                        <div class="list-summary-main">
                            <?php if (!empty($list['cover_poster_url'])): ?>
                                <img src="<?= htmlspecialchars($list['cover_poster_url']) ?>" class="list-summary-cover" alt="<?= htmlspecialchars($list['nome']) ?>">
                            <?php else: ?>
                                <div class="list-summary-cover list-summary-cover--empty">
                                    <i class="bi bi-folder-fill"></i>
                                </div>
                            <?php endif; ?>
                            <div class="list-summary-meta">
                                <h5 class="fw-bold mb-1 text-white text-truncate" style="font-size: 1rem;"><?= htmlspecialchars($list['nome']) ?></h5>
                                <div style="font-size: 0.75rem; color: #c9c9dd;">
                                    <?= (int)($list['item_count'] ?? 0) ?> itens
                                </div>
                                <div style="font-size: 0.7rem; color: #8f8fb0; margin-top: 4px;">Toque para abrir a lista</div>
                            </div>
                        </div>
                        <button class="btn btn-link text-danger p-0 flex-shrink-0" onclick="event.stopPropagation(); confirmDeleteList(<?= (int)$list['id_lista'] ?>, '<?= htmlspecialchars(addslashes($list['nome'])) ?>')" title="Excluir lista">
                            <i class="bi bi-trash fs-5"></i>
                        </button>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>

    <div style="height: 120px;"></div>
</div>

<div class="modal fade" id="createListModal" tabindex="-1" aria-hidden="true" style="z-index: 5000;">
    <div class="modal-dialog modal-dialog-centered" style="max-width: 360px; margin: auto 15px;">
        <div class="modal-content text-white p-4" style="border-radius: 20px; background-color: #1a1a24; border: 1px solid #2d2d3e; box-shadow: 0 10px 30px rgba(0,0,0,0.5);">
            <div class="d-flex align-items-center gap-2 mb-3">
                <i class="bi bi-folder-plus text-accent fs-4" style="color: #a88beb;"></i>
                <h5 class="fw-bold mb-0 text-white">Criar Nova Lista</h5>
            </div>
            <p class="small mb-4" style="font-size: 0.78rem; color: #d6d6ea !important;">Exemplo: "Para Assistir no Fim de Semana", "Favoritos de Ação".</p>
            <div class="mb-4">
                <input type="text" id="newListNameInput" class="form-control text-white border-0 py-2 px-3" style="background-color: #0c0c12; border-radius: 10px; font-size: 0.85rem;" placeholder="Nome da lista..." required>
            </div>
            <div class="d-flex gap-2">
                <button type="button" class="btn btn-secondary rounded-pill w-50 small fw-bold py-2" data-bs-dismiss="modal" style="background-color: #2c2c3e; border: none; font-size: 0.78rem;">Cancelar</button>
                <button type="button" class="btn rounded-pill w-50 small fw-bold py-2 text-white" onclick="submitCreateList()" style="background-color: #7c4dff; border: none; font-size: 0.78rem;">Criar</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="listItemsModal" tabindex="-1" aria-hidden="true" style="z-index: 5000;">
    <div class="modal-dialog modal-dialog-centered" style="max-width: 420px; margin: auto 12px;">
        <div class="modal-content text-white p-3" style="border-radius: 22px; background-color: #171720; border: 1px solid #2d2d3e; box-shadow: 0 18px 40px rgba(0,0,0,0.45);">
            <div class="d-flex align-items-center justify-content-between px-2 pt-2 pb-3">
                <div>
                    <h5 id="listItemsModalTitle" class="fw-bold mb-1 text-white">Lista</h5>
                    <div id="listItemsModalCount" style="font-size: 0.72rem; color: #bdbdd6;">0 itens</div>
                </div>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div id="listItemsModalBody" class="px-2 pb-2">
                <div class="text-center py-4 text-muted small">Carregando...</div>
            </div>
        </div>
    </div>
</div>
